# Web Shell 使用手册

Web Shell 是一个通过浏览器访问的远程终端服务。用户使用公司 AD 账号登录后，即可在网页中打开一个与本地终端一致的交互式命令行界面，无需安装任何客户端软件。本手册面向日常使用者与维护人员，说明服务的用途、使用方法与注意事项。

---

## 一、这是什么

Web Shell 解决的核心问题是：**让用户无需在本地安装终端工具或记住复杂的 SSH 命令，只需打开浏览器、用公司账号登录，就能安全地进入自己的 Linux 工作环境。**

它的主要特点：

- **随处可用**：任何装有现代浏览器的设备（电脑、平板）都能访问，无需额外客户端。
- **账号统一**：使用公司域账号（AD）登录，与邮箱、办公系统同一套账号体系。
- **两种工作模式**：既可进入普通的命令行终端（Bash），也可进入 AI 编程助手（OpenCode）界面。
- **个性化记忆**：你调整过的终端字体大小、配色主题，下次登录会自动恢复。
- **刷新不丢现场**：页面刷新或短暂断网后，正在运行的程序不会中断，回到浏览器即可继续。
- **文件上传**：可直接把本地文件上传到服务器当前目录。

---

## 二、开始使用

### 2.1 打开页面

在浏览器地址栏输入 Web Shell 的访问地址（由管理员提供），即可看到登录页面。

### 2.2 登录

登录页面需要填写：

| 项目 | 说明 |
| ---- | ---- |
| 用户名 | 你的域账号名（如 `zhangsan`），或公司邮箱（如 `zhangsan@company.com`） |
| 密码 | 你的域账号密码 |

填写完成后点击「登录」按钮。

> 请使用你自己的账号，Web Shell 会严格校验账号有效性；系统管理员账号（root）不允许登录。

### 2.3 选择工作模式

登录成功后，如果服务已启用 AI 助手，页面会提示你选择要进入的工作模式：

- **打开 Bash 终端**：进入常规的 Linux 命令行环境，可以执行任何日常操作。
- **打开 OpenCode**：进入 AI 编程助手界面，可在其中与 AI 协作编写代码、提问等。

根据需要选择其一即可。如果服务未启用 AI 助手，登录后将直接进入 Bash 终端。

---

## 三、终端界面说明

进入终端后，页面顶部有一排工具栏，自左向右分别是：

1. **标题**：显示 "Web Shell"。
2. **欢迎词**：居中的欢迎信息，会显示当前登录用户名。
3. **上传按钮**：将本地文件上传到当前目录（详见「文件上传」）。
4. **字号调整**：`A-` 减小字体、`A+` 增大字体，中间显示当前字号。
5. **主题选择**：下拉菜单可在多种配色主题间切换。

终端主体区域与本地命令行完全一致，支持粘贴、复制、上下键历史、Tab 补全等操作。

---

## 四、个性化设置

### 4.1 调整字体大小

- 点击工具栏的 `A+` 可放大字体，`A-` 可缩小字体。
- 字体大小会**自动记住**，下次登录自动恢复为你偏好的大小。

### 4.2 切换配色主题

- 点击工具栏的「主题」下拉菜单，可在多种流行配色间选择，例如深色（VS Code Dark、Dracula、Monokai、Nord 等）或浅色（GitHub Light）。
- 切换即时生效，并**自动记住**，下次登录自动恢复。

---

## 五、刷新与断线恢复

在日常使用中，你可能遇到以下情况：

- 手动刷新页面（按 F5 或 Ctrl+R）
- 浏览器标签页意外关闭后重新打开
- 网络短暂波动导致连接中断

**无论哪种情况，Web Shell 都会尝试恢复你之前的终端现场**：正在运行的程序（如正在编辑的文件、执行中的命令）不会丢失，回到页面后自动继续。

需要注意：

- 恢复机制面向**当前登录会话**，若关闭整个浏览器标签页较久（超过一定时间），会话可能被系统回收，届时需重新登录。
- 若会话确实已失效，页面会回到登录界面，重新登录即可。

---

## 六、文件上传

### 6.1 上传文件

1. 点击工具栏的「上传」按钮。
2. 在弹出窗口中选择要上传的本地文件。
3. 页面会显示上传进度条，等待上传完成。
4. 上传成功后，文件会保存到**当前终端所在的目录**。

> 上传过程中可随时点击「取消上传」中止操作。

### 6.2 权限说明

上传前，系统会先检查当前目录是否可写：

- **可写**：直接开始上传。
- **不可写**：会弹出提示，提醒你在终端中先切换到有写权限的目录（例如用 `cd` 命令进入自己的家目录或工作目录），再重新上传。

> 文件以上传者的身份保存，归属与权限由系统自动处理，请勿在无权限的目录（如系统目录）中强行上传。

---

## 七、退出登录

- 在终端中直接输入 `exit`（或按 `Ctrl+D`）退出当前 Bash 会话，即可回到登录界面。
- 在 OpenCode 界面退出后，同样会结束本次登录会话。
- 直接关闭浏览器标签页也可以结束会话（稍后系统会自动回收资源）。

---

## 八、常见问题（FAQ）

**Q1：登录时提示"用户不存在或未授权"？**
请确认账号名/邮箱输入正确，且该账号已被管理员授权使用 Web Shell。

**Q2：登录后没看到 OpenCode 选项，只有 Bash？**
说明当前服务未启用 AI 助手模式，仅提供普通命令行。可联系管理员确认。

**Q3：刷新页面后，之前运行的命令没了？**
请稍等片刻再刷新，或检查是否关闭标签页过久导致会话超时。若仍不行，重新登录即可。

**Q4：上传文件提示"无法写入当前目录"？**
当前目录没有写权限。请在终端中执行 `cd` 切换到你有写权限的目录（如 `cd ~` 回到家目录）后重试。

**Q5：终端字体太小/配色不喜欢？**
使用工具栏的 `A+`/`A-` 调整字号，或用「主题」下拉切换配色，设置会自动保存。

**Q6：忘记密码怎么办？**
Web Shell 使用公司域账号密码，找回密码请联系公司 IT 或按公司统一流程处理。

---

## 九、使用须知与安全提示

- 请妥善保管自己的账号密码，勿与他人共享。
- Web Shell 会以**你的身份**在服务器上执行命令，请像在本地终端一样谨慎操作，尤其注意删除、覆盖等不可逆命令。
- 上传文件时请确认目标目录与文件内容，避免将敏感文件上传到不合适的目录。
- 工作会话默认在当前登录期间有效，重要操作完成后建议及时退出登录，避免他人误用。

---

## 十、联系支持

如在使用过程中遇到无法解决的问题，请提供以下信息联系管理员：

- 使用的账号名
- 出现问题的时间与具体操作步骤
- 页面上的错误提示内容（可截图）

管理员会根据这些信息协助排查。
